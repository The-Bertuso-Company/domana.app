import React from "react";
import { View, Text } from "react-native";

/**
 * Placeholder so the app compiles.
 * Later we’ll swap for react-native-maps with price markers.
 */
export default function MapViewDomana() {
  return (
    <View style={{ flex: 1, backgroundColor: "#dbeafe", borderRadius: 12, alignItems:"center", justifyContent:"center" }}>
      <Text>Map goes here</Text>
    </View>
  );
}
